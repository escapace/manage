#!/usr/bin/env bash

[[ $LIBEXEC_MANAGE_LOADED ]] && return

source libexec-functions
source libexec-arguments
source libexec-cache

_ () (
    local ___one="$1"
    shift

    _"${___one}" "$@" || return $?
    return $?
)

LIBEXEC_MANAGE_LOADED=1

